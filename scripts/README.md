# Scripts

Small scripts written while learning — not meant to be polished software, just applied practice.

Each script gets its own folder with:
- the script itself
- a short `README.md` explaining: what it does, why I built it, how to run it

## Ideas to build toward (check off as done)
- [ ] Simple port scanner (Python)
- [ ] Log parser that flags failed login attempts from a sample log file
- [ ] Script that checks for common weak configs (e.g. default passwords list check)
- [ ] Basic phishing email indicator checker (keyword/header flags)

> Don't worry about these being advanced — the point is showing I can apply code to a security problem, however small.
